# Optional — app runs on in-memory mock data without a database.
# Uncomment when wiring API routes to Prisma + Neon/Postgres.

# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
