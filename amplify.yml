version: 1
applications:
  - appRoot: lukky-web
    frontend:
      phases:
        preBuild:
          commands:
            - nvm use 22
            - npm ci
            - npx prisma generate
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
